name: Skeptic Gate
on:
  pull_request:
    types: [opened, synchronize, edited, reopened]
  workflow_dispatch:
concurrency:
  group: skeptic-gate
  cancel-in-progress: false
jobs:
  # Renamed from skeptic_gate to avoid job-name collision with skeptic-gate.yml's
  # skeptic_gate job (which contains the real gate-checking steps). Both workflows
  # share name:"Skeptic Gate" so they merge — the second skeptic_gate definition
  # overwrites the first, causing the gate to silently run cron steps instead.
  skeptic_gate_check:
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write
      issues: write
      contents: read
    outputs:
      trigger_updated: \${{ steps.post_trigger.outputs.trigger_updated }}
      trigger_sha: \${{ steps.post_trigger.outputs.trigger_sha }}
    steps:
      - name: Post skeptic trigger comment
        id: post_trigger
        env:
          GITHUB_TOKEN: \${{ secrets.GITHUB_TOKEN }}
          HEAD_SHA: \${{ github.event.pull_request.head.sha }}
        run: |
          PR_NUM=\${{ github.event.pull_request.number }}
          echo trigger_updated=$(date -u +%Y-%m-%dT%H:%M:%SZ) >> \$GITHUB_OUTPUT
          echo trigger_sha=\$HEAD_SHA >> \$GITHUB_OUTPUT
      - name: Poll for skeptic VERDICT
        env:
          GITHUB_TOKEN: \${{ secrets.GITHUB_TOKEN }}
          TRIGGER_UPDATED: \${{ steps.post_trigger.outputs.trigger_updated }}
          TRIGGER_SHA: \${{ steps.post_trigger.outputs.trigger_sha }}
        timeout-minutes: 2
        run: |
          echo waiting
