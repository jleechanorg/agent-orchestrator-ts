# Lifecycle Real Conflict Resolution Fixture

Base branch text: keep the base-side lifecycle observation language.

Resolution expectation: the AO worker should preserve this base-side note while combining it with the head-side note.
