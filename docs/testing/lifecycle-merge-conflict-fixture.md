# Lifecycle Merge Conflict Fixture

This is the HEAD branch version of the docs-only merge conflict fixture.

It intentionally conflicts with the temporary base branch so lifecycle handling can observe a merge-conflicted PR.
