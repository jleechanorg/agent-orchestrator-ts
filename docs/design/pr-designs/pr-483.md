# [agento] fix(gates): rescue skeptic gate timing and bot author skips

## Overview

- fix the GitHub App/bot-author Gate 3 bypass regex across the green gate, skeptic cron, and reusable skeptic gate workflows so both real GitHub App logins like `app/coderabbitai` and hyphenated bot logins such as `github-actions[bot]` are recognized
- extend `Skeptic Gate CI` polling headroom so delayed skeptic verdict comments do not fail closed before the verdict arrives
- align CLI skeptic merge-gate state and skeptic prompt guidance with the workflow behavior for bot-authored PRs
- add structural and CLI tests covering the widened bot regex, review-skipped Gate 3 handling, and the longer skeptic polling window

**PR:** [#483](https://github.com/jleechanorg/agent-orchestrator/pull/483) · **Status:** **Open** · **Date:** April 23, 2026

**Author:** @app/coderabbitai

---

## Metadata

| Metric | Value |
|--------|-------|
| Files changed | 12 |
| Head branch | `coderabbitai/chat/2b09b7a` |
| Scope | workflow gates + skeptic CLI/tests |

---

## Files Changed

| File | Why |
|---|---|
| `.beads/issues.jsonl` | track the rescue task bead opened for PR 483 |
| `.github/workflows/green-gate.yml` | widen bot-author skip regex for Gate 3 |
| `.github/workflows/skeptic-cron.yml` | widen bot-author skip regex in cron gate evaluation |
| `.github/workflows/skeptic-cron-reusable.yml` | keep reusable cron logic in sync with repo workflow |
| `.github/workflows/skeptic-gate-reusable.yml` | widen bot-author skip regex in reusable skeptic gate |
| `.github/workflows/test.yml` | give skeptic verdict polling enough headroom for delayed comment posting |
| `packages/cli/src/commands/skeptic/mergeGate.ts` | report bot-authored PRs as review-skipped for Gate 3 |
| `packages/cli/src/commands/skeptic/prompt.ts` | instruct the skeptic model to treat bot-authored PRs as review-skipped |
| `packages/cli/__tests__/commands/skeptic/mergeGate.test.ts` | lock in merge-gate review-skipped behavior |
| `packages/cli/src/__tests__/skeptic/skeptic-structured-output.test.ts` | lock in prompt guidance for bot-author Gate 3 skips |
| `packages/core/src/__tests__/wholesome.test.ts` | structural checks for widened bot regex + skeptic timeout headroom |

---

## Verification Notes

- Red: wholesome structural assertions failed because the workflow regex still used patterns that missed real GitHub App logins (`app/coderabbitai`) and the skeptic polling window was too short
- Green: the updated workflow/CLI tests pass after switching the shell regex to `^(app/.+|[[:alnum:]_-]+\[bot\])$`, keeping CLI Gate 3 skip logic aligned, surfacing `review skipped (...)` in the skeptic prompt, and extending skeptic polling headroom

Generated manually to reflect the actual PR diff.
