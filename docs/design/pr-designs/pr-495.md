# [agento] fix: skip skeptic cron trigger when FAIL/SKIPPED verdict exists for current SHA

## Overview

**PR:** [#495](https://github.com/jleechanorg/agent-orchestrator/pull/495) · **Status:** Open · **Date:** April 24, 2026

**Author:** @jleechan2015

Fixes a trigger-spam bug in the skeptic cron reusable workflow. When a FAIL or SKIPPED verdict already exists for the current PR head SHA, the cron was still posting new trigger comments every cron cycle, causing repeated re-evaluations of the same failed PR.

---

## Problem

`skeptic-cron-reusable.yml` had dedup checks for:
1. Existing PASS verdict → skip (already present)
2. Existing unresolved trigger request-id → skip (already present)

But NOT:
3. Existing FAIL/SKIPPED verdict → skip (MISSING — bug)

This caused PRs that had already received a FAIL verdict to be re-triggered on every cron cycle (~30-60 min), generating redundant skeptic evaluations and comment noise.

---

## Fix

Added a jq filter using the same SHA marker anchoring as existing checks:
- `<!-- skeptic-cron-trigger-${SHA} -->` — ties the verdict to the exact cron trigger that requested it
- `<!-- skeptic-head-sha-${SHA} -->` — ties the verdict to the exact commit

When both markers match and the verdict is FAIL or SKIPPED, the cron continues to the next PR without posting a new trigger.

---

## Architecture

```text
skeptic-cron-reusable.yml — trigger dedup flow:
  1. Check for existing PASS verdict for current SHA  → skip if found
  2. CHECK FOR EXISTING FAIL/SKIPPED VERDICT (NEW)    → skip if found
  3. Check for unresolved trigger request-id          → skip if found
  4. Post new trigger comment
```

---

## Test Coverage

TypeScript behavior tests in `packages/core/src/__tests__/jq-filters.test.ts`:

| Test | Validates |
|------|-----------|
| detects existing FAIL verdict and suppresses retrigger | FAIL → count=1 |
| detects existing SKIPPED verdict and suppresses retrigger | SKIPPED → count=1 |
| does not suppress when existing verdict is PASS | PASS → count=0 |
| does not suppress when FAIL verdict is for a different SHA | Wrong SHA → count=0 |
| does not suppress when no verdict exists | Empty → count=0 |

---

## Metadata

| Metric | Value |
|--------|-------|
| Files changed | 2 |
| Head branch | `dev1777014885` |
| Created | April 24, 2026 |
| Claim class | unit (test helpers + GHA workflow fix) |
