# [agento] feat: source API keys from shell init files at daemon startup (bd-g884)

## Overview

**PR:** [#528](https://github.com/jleechanorg/agent-orchestrator/pull/528) · **Status:** Open · **Date:** May 7, 2026

**Author:** @jleechan2015

This PR adds configurable `envSource` support to the AO config, allowing the daemon to source API keys (MINIMAX_API_KEY, ANTHROPIC_API_KEY, etc.) from shell init files at startup. This solves the root cause: the AO daemon starts without a login shell and never inherits env vars defined in `~/.bashrc`.

---

## Problem

The AO daemon does NOT inherit from `~/.bashrc` when it starts — it launches as a non-login, non-interactive process. The MiniMax plugin (and other plugins) read `process.env.MINIMAX_API_KEY`, which is always `undefined` in worker sessions unless the key was exported to the launchd environment.

Users who define `export MINIMAX_API_KEY=...` in their `~/.bashrc` (or `~/.zshrc`, `~/.profile`) have no way to expose it to AO worker sessions without duplicating secrets into the YAML config file.

---

## Solution

### envSource config field

A new `envSource` field in `OrchestratorConfig` (top-level or under `defaults`) accepts a list of shell init file paths to source at config-load time:

```yaml
defaults:
  runtime: tmux
  agent: claude-code
  envSource:
    - ~/.bashrc
```

Default: `["~/.bashrc"]`.

### sourceEnvFile() — shell sourcing utility

The new `applyEnvSource(files)` function in `packages/core/src/env-source.ts` runs for each configured file:

```bash
bash --noprofile --norc -i -c 'source "$1" > /dev/null 2>&1; env' -- <expanded-path>
```

Three shell flags are intentional:
- `--noprofile` — do not source `/etc/profile` or login scripts before interactive startup
- `--norc` — do not source `~/.bashrc` implicitly before the explicit `source "$1"`; prevents implicit startup-file leakage where bash sources `~/.bashrc` automatically before the configured file exports vars
- `-i` — interactive mode so that `[[ $- != *i* ]] && return` guards in common `.bashrc` templates do not skip the API-key exports below them

A prefix-matched diff against a pre-source `process.env` snapshot ensures only new/changed vars are merged, preventing PATH/PS1 pollution.

### Security: path allowlist

`envSource` schema validation rejects any path that is not a shell dotfile under `~` or `/etc/environment`. The regex:

```
^~\/.[a-zA-Z][a-zA-Z0-9_-]*$
```

Requires a literal dot after `~/`, which blocks:
- `/tmp/evil.sh` (absolute path)
- `~/scripts/env.sh` (non-dotfile under home)
- `~/../otheruser/evil.sh` (path that escapes `$HOME`)
- `~/bashrc` (unescaped dot — `.bashrc` requires the dot)

### Allowed prefixes

Only env vars with known API-key prefixes are imported:

```typescript
const ALLOWED_PREFIXES = [
  "MINIMAX_",
  "ANTHROPIC_",
  "OPENAI_",
  "MCP_AGENT_MAIL_",
  "AO_",
] as const;
```

---

## Implementation

### Files changed

| File | Role |
|------|------|
| `packages/core/src/env-source.ts` | New module: `sourceEnvFile()` + `applyEnvSource()` |
| `packages/core/src/config.ts` | Add `envSource` to schema; call `applyEnvSource()` at `loadConfig()` |
| `packages/core/src/__tests__/env-source.test.ts` | 21 unit tests for prefix filtering, diff logic, `;` separator |
| `packages/core/src/__tests__/config-validation.test.ts` | 9 new tests for envSource schema security |
| `packages/plugins/agent-minimax/src/index.ts` | Better logging with envSource attribution |

### applyEnvSource call site (config.ts)

```
bootstrapEnvSource(config) called once per process at loadConfig() time
preference: config.defaults?.envSource ?? config.envSource ?? ["~/.bashrc"]
```

Called from `loadConfig()` after schema validation, before plugins load. The single-call-per-process invariant is enforced by the module-level `ENV_BEFORE` snapshot captured at import time.

### Shell separator: `;` not `&&`

The sourcing command uses `;` (semicolon) not `&&`:

```bash
source "$1" > /dev/null 2>&1; env
```

`&&` short-circuits — if the sourced file exits non-zero (e.g. `set -e`, a failing command), `env` never runs and no vars are captured. `;` always runs `env` regardless of exit code.

---

## Compatibility Notes

### Interactive shell flag (`-i`)

Using `bash -i -c` means the sourced file runs as if in an interactive shell. This is intentional:
- Common `.bashrc` templates guard exports with `[[ $- != *i* ]] && return` — without `-i` those exports are skipped.
- Interactive sourcing may also source `~/.bash_profile` (which sources `~/.bashrc`), so vars may be loaded twice — the diff guard prevents duplicates.

### File order and override

`applyEnvSource` calls `sourceEnvFile` for each file in sequence, passing a fresh `process.env` snapshot before each file. Later files override earlier ones for the same key.

### Non-interactive-only setups

Users who run commands in a completely non-interactive context (no TTY, no `bash -i`) should use `/etc/environment` (the only absolute path allowed by schema). `/etc/environment` is a simple `KEY=VALUE` file with no shell syntax and no guards.

---

## Regression Tests

### env-source.test.ts (21 tests)

| Test | What it validates |
|------|-------------------|
| Returns MINIMAX_ vars from sourced output | prefix match |
| Returns ANTHROPIC_ vars | prefix match |
| Returns OPENAI_ vars | prefix match |
| Returns MCP_AGENT_MAIL_ vars | prefix match |
| Returns AO_ vars | prefix match |
| Ignores vars without matching prefix | filter correctness |
| Does not mutate process.env directly | immutability contract |
| Calls execFileSync with correct args | bash invocation |
| Returns empty object for missing file | graceful degradation |
| Returns empty on execFileSync error | error recovery |
| Respects custom beforeSnapshot | override ordering |
| Multi-file later-overrides-earlier | ordering |
| Uses semicolon separator so env runs even when sourced file exits non-zero | `;` vs `&&` |
| Correct number of ALLOWED_PREFIXES entries | completeness |

### config-validation.test.ts (47 tests, 9 envSource)

| Test | What it validates |
|------|-------------------|
| accepts a single envSource file | schema acceptance |
| accepts multiple envSource files | schema acceptance |
| accepts /etc/environment | absolute exception |
| rejects absolute path | security |
| rejects non-dotfile path under home | security |
| rejects path that escapes home directory | security |
| rejects absolute path in defaults.envSource | security |
| accepts shell dotfile in defaults.envSource | defaults path |
| uses regex that requires escaped dot | escaping correctness |

---

## Risk And Mitigation

| Risk | Mitigation |
|------|------------|
| `-i` flag causes bash to hang or wait for input | `stdin: pipe` in execFileSync + `> /dev/null` redirect |
| `~/.bashrc` sources `~/.bash_profile` → infinite loop | execFileSync timeout (10s) catches runaway loops |
| Malicious envSource config path | schema `.refine()` allowlist blocks all absolute paths except `/etc/environment` |
| API key leaked in process.env snapshot | allowed-prefix filter prevents non-API vars from being merged |
| Daemon startup blocked by bad envSource | try/catch in `sourceEnvFile` — failure is silent, no throw |

---

## Metadata

| Metric | Value |
|--------|-------|
| Files changed | 5 |
| Head branch | `feat/bd-g884` |
| Claim class | unit + security fix |
| Bug | bd-g884 |
