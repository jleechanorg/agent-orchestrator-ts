# [agento] fix(session-manager): skip main project dir in pruneStaleWorktrees Pass 2

## Overview

N/A - governance: infrastructure fix
N/A - bead: emergency fix for data loss bug

**PR:** [#642](https://github.com/jleechanorg/agent-orchestrator/pull/642) · **Status:** **Open** · **Date:** May 29, 2026


**Author:** @jleechan2015

---

## Architecture

<pre>
┌───────────────────────────────────────────────────────────────────────────── ┐
│ PR #642 — agent-orchestrator                                              │
└───────────────────────────────────────────────────────────────────────────── ┘
                  │
    ▼ 2 file(s) changed
                  │
┌───────────────────────────────────────────────────────────────────────────── ┐
│ fix(session-manager): skip main project dir in pruneStaleWorktrees Pass 2 │
└───────────────────────────────────────────────────────────────────────────── ┘
</pre>

---

## Metadata

| Metric | Value |
|--------|-------|
| Files changed | 2 |
| Commits | 2 |
| Additions | +63 |
| Deletions | -0 |
| Head branch | `fix/prune-stale-skip-main-worktree` |
| Created | May 29, 2026 |


---

## Files Changed (by package)

| Package / Path | Files |
|---|---|
| `packages/core` | 2 |

---

## All Files Changed

| File | Status | + | − |
|---|---|---|---|
| `packages/core/src/__tests__/session-manager-prune-stale-worktrees.test.ts` | modified | +57 | -0 |
| `packages/core/src/session-manager.ts` | modified | +6 | -0 |

---

Generated by `scripts/generate-pr-design-docs.mjs` · [View PR #642 on GitHub](https://github.com/jleechanorg/agent-orchestrator/pull/642)

## Evidence

### TDD Red-Green Cycle Evidence

- **Red Phase (Bug/Gap Demonstrated)**: The regression test `Pass 2: should NOT delete the main project directory when a killed session references project.path as worktree` failed because without the path equality guard in `pruneStaleWorktrees()` Pass 2, the `git worktree remove` command was attempted on the project root, violating repository integrity.
  ```text
  FAIL  src/__tests__/session-manager-prune-stale-worktrees.test.ts > pruneStaleWorktrees > Pass 2: should NOT delete the main project directory when a killed session references project.path as worktree
  AssertionError: expected [ Array(1) ] to not include '/var/folders/j0/byd1z6px50v88lf679bgt…'
   ❯ src/__tests__/session-manager-prune-stale-worktrees.test.ts:496:30
      494|     // Guard must prevent deletion of the main project dir,
      495|     // so the main repo path should not be removed as a worktree, and …
      496|     expect(removedPaths).not.toContain(mainRepoPath);
         |                              ^
      497|     expect(existsSync(mainRepoPath)).toBe(true);
      498|   });
  ```
- **Green Phase (Fix Verified)**: Once the `normalizedWorktree === normalizePath(repoPath)` path guard was added, the `removedPaths` array no longer contained the project root, and the test successfully passed.
  ```text
  ✓ src/__tests__/session-manager-prune-stale-worktrees.test.ts (10 tests) 16ms

   Test Files  1 passed (1)
        Tests  10 passed (10)
     Start at  12:47:21
     Duration  232ms (transform 118ms, setup 0ms, import 141ms, tests 16ms, environment 0ms)
  ```

### Local Test Verification

- **Command Run**: `pnpm -C packages/core test session-manager-prune-stale-worktrees`
- **Result**: All 10 tests passed successfully.

