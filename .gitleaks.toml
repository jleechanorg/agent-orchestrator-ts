# SECURITY.md uses illustrative env examples; legacy root agent-orchestrator.yaml
# contained sample tokens in history. Scan current tree strictly; allowlist doc-only paths.
title = "agent-orchestrator"

[extend]
useDefault = true

[allowlist]
paths = [
  '''^SECURITY\.md$''',
  '''^agent-orchestrator\.yaml$''',
]
